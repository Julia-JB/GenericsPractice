// Number subclasses: Byte, Short, Integer, Float, etc - wrapper classes around numeric primitive types

// The constraint can also be an interface <T extends Comparable> or <T extends Comparable & Clonable>.
// Meaning that in the later the generic can only store objects that are comparable and clonable.
public class GenericsConstraints <T extends Number> {

}
